import { Request, Response } from "express";
import { CitasMedicasService } from "../lib/services/citas-medicas.service";

export class CitasMedicasController {
  public static async listarCitaMedica(req: Request, res: Response) { // callable by frontend
    // TODO
  }
}
